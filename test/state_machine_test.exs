defmodule StateMachineTest do
  use ExUnit.Case
  doctest StateMachine
  # alias StateMachine.{State, Event}

  # test "creates the structure" do
  #   sm = %StateMachine{}
  #   assert sm.states == []
  #   assert sm.events == []
  # end

end
